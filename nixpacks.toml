[phases.setup]
nixPkgs = ["nodejs_22", "git", "python3"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["node scripts/build.js"]

[start]
cmd = "node_modules/.bin/tsx server/index.ts"
